<div class="form-group">
	<label>{{ localize "spell-class-filter-for-5e.SpellClass" }}</label>
	<select name="flags.spell-class-filter-for-5e.parentClass">
		{{#select flags.spell-class-filter-for-5e.parentClass}}
		<option value="">---</option>
		{{#each SCF as |name key|}}
		<option value="{{key}}">{{ localize name}}</option>
		{{/each}} {{/select}}
	</select>
</div>
